# Copy to all.yml (gitignored) or supply via --extra-vars / GitHub Actions secrets.
postgres_password: CHANGE_ME
domain: maps.example.com
presenting_ip: "203.0.113.10"  # terraform output presenting_public_ip
processing_ip: "203.0.113.11"  # terraform output processing_public_ip
ghcr_username: malkahtani1981
# ghcr_token: supplied by CI as secret
