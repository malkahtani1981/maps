---
- name: UFW — allow HTTP/HTTPS
  ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  loop: ["80", "443"]

- name: Create stack directory
  file:
    path: /opt/maps/app
    state: directory
    owner: ubuntu

- name: Render docker-compose for app stack
  template:
    src: docker-compose.yml.j2
    dest: /opt/maps/app/docker-compose.yml
    owner: ubuntu
  notify: restart app stack

- name: Render Caddyfile (automatic TLS)
  template:
    src: Caddyfile.j2
    dest: /opt/maps/app/Caddyfile
    owner: ubuntu
  notify: restart app stack

- name: Log in to GitHub Container Registry
  community.docker.docker_login:
    registry_url: ghcr.io
    username: "{{ ghcr_username }}"
    password: "{{ ghcr_token }}"
  when: ghcr_token is defined

- name: Pull latest images & start app stack
  community.docker.docker_compose_v2:
    project_src: /opt/maps/app
    state: present
    pull: always
